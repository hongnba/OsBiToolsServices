# Disable banner
spring.main.banner-mode=off

# Home directory with configuration
# Must be same value as TestConstants.WORK_OSBI_SHARED
home.dir=target/osbi_shared

ws.config.debug=true
ws.config.trace=true
ws.config.lang=en
ws.config.rescan=500

dev.enabled=true

# Custom debug level
# logging.level.com.osbitools.ws.rest=DEBUG
